# DECISIONS

| Time | Module | Decision | Reason | Receipt |
|---|---|---|---|---|
| 2026-07-28T11:00:00-04:00 | architecture | The target-architecture migration is complete when exact ownership, collision, build, static safety, default test, and explicit Admin-emulator gates all pass; historical checkpoint failures do not remain open after their stale assertions are repaired. | The last three reds were a superseded approval-copy expectation, two local-loop tests that omitted the returned opaque approval state, and a fixed calendar expiry. Updating those tests to the current contract exposed no product-code failure. The blueprint scanner also now recognizes the approved human-readable `Site job blueprint` name. | `receipts/architecture/target-architecture-final-proof.txt` |
| 2026-07-28T10:00:00-04:00 | architecture | Contact owns client selection and form derivation, NexCam owns capture-surface assembly, and NexOps Workspace retains only the cross-area actions each owner calls. | Derived records, form synchronization, capture filtering, and capture props change with their feature. Route transitions and the active module remain shared shell concerns. Passing one controller contract into the NexCam owner avoids copying state while removing feature-specific assembly from the shell. | `apps/web/src/features/clients/components/contact/hooks/useContactWorkspaceModel.ts`, `apps/web/src/features/nexcam/areas/capture/components/NexOpsCaptureModule.tsx`, `receipts/architecture/workspace-owner-composition.txt` |
| 2026-08-01T00:00:00-04:00 | payments/platform | Each tenant bears Stripe’s own processing fees. NexTeam charges no application/platform fee, does not subsidize fees, and does not pool them. | Stripe Connect direct charges run on the tenant’s Express connected account; platform fee fields are prohibited. | `apps/server/src/modules/nexops/areas/invoices/components/paymentRails/server/stripe.ts`, `apps/server/src/platform/routes.ts` |
| 2026-07-27T14:50:00-04:00 | architecture | NexOps API/record contracts and deterministic workspace support are separate from the stateful workspace orchestrator; the existing workspace module may forward exports but cannot retain duplicate bodies. | Record shapes and pure formatting/draft behavior can change and be tested without touching route state, effects, or composition. Keeping only forwarding exports preserves existing component imports while reducing the orchestrator by more than one thousand real lines. | `apps/web/src/features/nexopsShell/contracts/workspaceContracts.ts`, `apps/web/src/features/nexopsShell/workspaceSupport.tsx`, `receipts/architecture/nexops-workspace-support.txt` |
| 2026-07-27T14:20:00-04:00 | architecture | Legacy global CSS is partitioned by selector ownership while one implementation-free manifest preserves the original cascade order. | Moving all rules into a differently named monolith would not improve parallel editing, while independently importing each owner stylesheet could accidentally reorder the cascade. The manifest keeps behavior stable and lets component CSS change without reopening a 4,468-line root file. | `apps/web/src/shared/app/globalStyles.ts`, `receipts/architecture/root-debt-css-decomposition.txt` |
| 2026-07-27T13:45:00-04:00 | architecture | The mixed deferred web module is split by behavior owner with no root facade: capture belongs to NexCam, client creation to Contact, and create-menu/notifications to NexOps Shell. | The four surfaces change for different product reasons. Labeling the whole file as shared would preserve false coupling, while retaining a re-export facade would keep the root dependency alive. Direct owner imports make later edits and collision proof truthful. | `receipts/architecture/root-debt-deferred-ui.txt` |
| 2026-07-27T13:15:00-04:00 | architecture | Legacy root web files with one clear owner move directly into that owner; genuinely cross-area mechanisms become named shared components rather than compatibility facades. | Keeping root helpers on the collision allowlist hid their change ownership. Git renames preserve the implementation without duplication, while explicit shared roots make cross-area dependencies visible. The mixed deferred UI is excluded from this move because it contains four different owners and must be split rather than mislabeled. | `receipts/architecture/root-debt-single-owner-web.txt` |
| 2026-07-27T12:30:00-04:00 | security | Field Docs and NexDocs Admin writes enforce tenant ownership inside the repository transaction, and update contracts require the caller tenant rather than accepting an ID alone. | Firebase Admin bypasses Firestore rules. A tenant-filtered read before an unscoped write leaves both cross-tenant ID collisions and time-of-check/time-of-use exposure. Reading and writing the target document in one transaction rejects conflicting ownership atomically; forcing the authenticated tenant into the resulting record prevents tenant-ID mutation. | `apps/server/src/fielddocs/mediaRepository.ts`, `apps/server/test/fielddocs-tenant-boundary.test.mjs`, `receipts/architecture/admin-isolation-fielddocs.txt` |
| 2026-07-27T03:00:00-04:00 | architecture | Component registrars destructure only the dependencies their handlers execute; broad runtime context types remain composition contracts, not copied local binding lists. | Repeating every context member in every registrar created 2,724 false local dependencies and obscured each component's real coupling. Narrow destructuring keeps runtime composition compatible while making implementation dependencies reviewable and lint-clean. | `receipts/architecture/d2-lint-cleanup.txt` |
| 2026-07-27T02:55:00-04:00 | security | Request reopen must tenant-load the request before using the repository's legacy ID-only update method. | Lint exposed that the route computed a tenant but did not use it. Firebase Admin bypasses browser rules, so discarding the variable would retain a cross-tenant mutation edge; the existing tenant-scoped lookup now fails closed before mutation. | `apps/server/src/modules/nexops/areas/requests/components/requestCore/server/routes.ts`, `receipts/architecture/d2-lint-cleanup.txt` |
| 2026-07-27T02:00:00-04:00 | architecture | A declaration shared by the root and NexOps shell has one implementation owner; NexOps-specific declarations remain in `NexOpsWorkspace`, while root-only Nexi, Schedule, Reputation, and NexReach declarations remain in `main.tsx`. | The shell extraction left same-named implementations in both files. Removing true copies and importing the still-shared implementation preserves behavior while making ownership explicit. The AST audit found 65 declaration overlaps rather than the earlier regex count of 59 because it recognizes six additional declaration forms; the post-cleanup count is zero. Auth/session helpers remain temporarily Workspace-owned for D1 and move to the shared Auth boundary in R3. | `receipts/architecture/d1-duplicate-declarations.txt` |
| 2026-07-26T00:00:00-04:00 | shared | Numbering owns prefix-agnostic format/advance mechanics, while tenant/component settings own request, quote, job, invoice, and receipt formats. Reservations serialize per tenant in memory and transact on the tenant's Firestore settings document. | All sequence kinds share one settings document. Locking only per kind allowed a simultaneous quote reservation to overwrite an advanced receipt rule, so the in-memory boundary must serialize the whole tenant settings mutation. Firestore transactions provide the equivalent retry/atomicity guarantee. Receipt is a first-class sequence per the resolved execution order; old four-rule documents receive a schema default instead of losing customized settings. | `packages/shared/src/numbering.ts`, `apps/server/src/shared/numbering/numberingService.ts`, `packages/providers/src/native/NativeAdapter.ts`, `apps/server/src/crm/nativeRepository.ts`, `receipts/architecture/e2-numbering.txt` |
| 2026-07-26T00:00:00-04:00 | shared | Document Rendering owns generic PDF writing and HTML escaping only; Quote Engine and Invoice Structure own their document wording as separate templates. | The rendering mechanism changes for technical reasons while quote and invoice content change for different business reasons. Keeping those templates outside the shared engine prevents a quote edit from contending with invoice policy. The legacy `crm/quotePdf.ts` facade remains temporarily to preserve import compatibility and will relinquish portal-template ownership during E4. | `apps/server/src/shared/documentRendering/`, `apps/server/src/modules/nexops/areas/quotes/components/quoteEngine/server/quotePdfTemplate.ts`, `apps/server/src/modules/nexops/areas/invoices/components/invoiceStructure/server/invoicePdfTemplate.ts`, `receipts/architecture/e2-document-rendering.txt` |
| 2026-07-26T00:00:00-04:00 | shared | Address/Location is a dependency-light `@nexteam/shared` component consumed by server and browser surfaces; `@nexteam/core` keeps its established `Address` type and re-exports the shared utilities for server compatibility. | Importing the broad core barrel into Vite increased the main browser chunk from `247.39 kB` to `341.91 kB`. The dedicated shared package keeps the final main chunk at `247.60 kB` while providing one independently editable implementation for parsing, formatting, geocode mapping, intake materialization, storage keys, and map links. Provider-backed geocoding and distance calls remain in the server Address/Location surface. | `packages/shared/src/addressLocation.ts`, `apps/server/src/shared/addressLocation/`, `receipts/architecture/e2-address-location.txt` |
| 2026-07-26T00:00:00-04:00 | architecture | Chat approval continuity is carried by the structured `pendingApproval` response/request field; approval IDs remain absent from user-visible assistant text and persisted message history. | Structured state is the authoritative boundary and avoids turning internal action identifiers into conversational data. The compatibility parser still understands legacy prompt wording and underscore-bearing IDs for old transcripts and direct callers. | `apps/server/src/nexi/nexiService.ts`, `packages/nexi/src/gateway.ts`, `receipts/architecture/e1-migration-baseline.txt` |
| 2026-07-26T00:00:00-04:00 | crm | A client update validates a complete address only when that update changes `billingAddress` or `primaryProperty`; consent-only updates preserve an existing legacy record as-is. | Requiring an unrelated address repair made consent revocation fail, even though revocation must remain immediately enforceable. New client creation and actual address edits keep their completeness checks. | `apps/server/src/crm/routes.ts`, `apps/server/test/request-foundation.test.mjs` |
| 2026-07-26T00:00:00-04:00 | scheduling | Date-based scheduling expectations use the current lifecycle derivation rather than freezing a July 2026 fixture as permanently `Upcoming`. | The fixture date is now in the past and the locked zero-day grace policy correctly derives `Late`; changing production logic to satisfy a stale date would be a regression. | `apps/server/test/schedule-home-activity.test.mjs` |
| 2026-07-26T00:00:00-04:00 | architecture | The target-architecture integration branch preserves the `f2fb766` live server and web shells during M3 compatibility work instead of replacing them with platform placeholder feature seams. | The nightly integration contains the real CRM/NexOps behavior. Replacing its shells would erase working functionality rather than modularize it. The platform module registry is adapted to the live Nexi request/tenant wiring first; later slices must move real code into canonical module boundaries. | `72488c3`, M3 build output, and baseline comparison: `339` integration tests with `324` pass / `15` inherited failures versus `337` baseline tests with `322` pass / `15` matching failures. |
| 2026-07-26T00:00:00-04:00 | security | Native CRM document updates must prove tenant ownership before calling a repository mutation and must reject a requested tenant-ID change. | Firebase Admin and repository implementations are not protected by Firestore Rules. The server-facing Native adapter now fails closed for quote, invoice, and job updates, including job-status changes, without revealing cross-tenant record existence. Repository APIs still need a later tenant-keyed mutation contract to remove time-of-check/time-of-use exposure for direct repository callers. | `packages/providers/src/native/NativeAdapter.ts`, `apps/server/test/tenant-isolation.test.mjs` (`1/1` passing). |
| 2026-07-22T00:30:00-04:00 | nexi | Nexi now defaults to model-driven natural-language understanding for freeform parsing, intent understanding, and response shaping; rigid regex/keyword classifiers are an audit-first conversion target rather than the default architecture. | Chris explicitly decided Nexi should reason like the underlying LLM, grounded in NexTeam data, instead of relying on brittle hand-rolled classifiers. Permanent boundaries remain unchanged: ApprovalQueue still gates actions, money/security stay deterministic, source-check discipline stays enforced, and the conversion must happen incrementally from the highest-live-pain paths outward. | Chris decision in-channel, July 21-22, 2026. Priority 1 client-create parsing lived in `packages/nexi/src/gateway.ts` `createClientAddressMatchFromText()`, `createClientNameFromText()`, `createClientInputFromText()`, and `normalizeToolInput()` plus `apps/server/src/nexi/nexiService.ts` `looksLikeCreateClientAction()`, `chooseTool()`, and `runExplicitLocalToolLoop()`. Priority 2 client-lookup parsing/routing lives in `packages/nexi/src/gateway.ts` `clientLookupQueryFromText()`, `deterministicToolNames()`, and `clientLookupAnswer()`; the server-local lane only contributes the broader query-routing heuristics in `apps/server/src/nexi/nexiService.ts` `entityQueryFromText()` and `chooseTool()`. Priority 3 shared switchboard logic lives in `packages/nexi/src/gateway.ts` `deterministicToolNames()` and async `normalizeToolInput()`, plus `apps/server/src/nexi/nexiService.ts` `chooseTool()`. Supporting tool surfaces remain in `apps/server/src/crm/nexiTools.ts` and `apps/server/src/approval/nexiTools.ts`. |
| 2026-07-22T00:00:00-04:00 | context | Personal-context delivery and directions now resolve from the currently logged-in operator seat, while dispatch/shop-origin questions still use the Aquatrace home base. | Chris's architecture requirement refined the older fixed-home-base rule: "email me" and "text me" must use the signed-in user's own stored contact info, and "from here / from my house / from me" directions must prefer live device geolocation or fall back to that user's profile address. The older Aquatrace home-base default still applies to non-personal office/dispatch prompts like "from the shop". | `apps/server/src/nexi/nexiService.ts`, `packages/nexi/src/gateway.ts`, `apps/web/src/nexiRequestContext.ts`, `apps/server/test/local-dev-auth.test.mjs`, `apps/server/test/nexi-job-desk.test.mjs`, `apps/web/test/nexi-request-context.test.mjs` |
| 2026-07-21T00:00:00-04:00 | nexi | Local testing keeps email-only auth for now. | Chris explicitly chose email-only local auth as the fastest way to keep live testing moving; this is intentional local-test behavior, not a regression against production-style login requirements. | Chris decision in-channel, July 21, 2026 |
| 2026-07-03T23:59:00-04:00 | core | Created safety branch `archive/pre-bible-main` and committed the visible dirty worktree before M0. | User pre-authorized archiving current main state and forbade reset/delete. | `865c57a`, then cleanup `b80a2b6`; net `415 files changed, 48003 insertions(+), 253 deletions(-)` versus `main`. |
| 2026-07-03T23:59:00-04:00 | core | Removed two credential-named Aquatrace reference text files from the archive branch index and ignored `docs/internal/clawdia/reference/**/*.txt`. | The archive commit summary exposed credential-artifact filenames; local history is considered burned until reviewed/rotated. | `b80a2b6` |
| 2026-07-04T10:29:45-04:00 | core | Credential incident confirmed in `docs/internal/clawdia/reference/aquatrace/aquatrace-wordpress-application-password.txt` and `docs/internal/clawdia/reference/aquatrace/aquatrace-wordpress-editor-login.txt`. | The first file contained a username/login identifier plus password value for a WordPress application-password credential; the second contained a username/login identifier plus password value for a WordPress editor login. Raw values are intentionally not copied into logs or chat. | `receipts/security/history-scan.txt` |
| 2026-07-04T10:29:45-04:00 | core | Recreated `archive/pre-bible-main` as a fresh orphan branch and deleted the tainted branch history from branch refs. | The original archive branch had secret-file history, so it is quarantined and must never be pushed. | Clean archive `a6c3886`; deleted tainted `build/core` `a94b3d3`, tainted `build/nexi` `45352a3`, and one Codex checkpoint ref containing the credential tree. |
| 2026-07-04T10:29:45-04:00 | core | Added `check:secret-history` to scan reachable refs before verification passes. | Working-tree secret scans are not enough after a credential incident; pushable history must also fail on credential-like paths or high-risk token patterns. | `scripts/check-secret-history.mjs`, `npm run verify` |
| 2026-07-04T10:29:45-04:00 | core | Firestore UID-specific admin rule remains blocked because the provided UID value is the literal placeholder `[PASTE UID HERE]`. | Writing a placeholder UID into rules would create false security evidence. Current rule stays locked to the existing Chris email/claim gate until the real Firebase UID is supplied. | `firestore.rules` |
| 2026-07-04T10:29:45-04:00 | core | Rotated Jobber and CompanyCam smoke receipts are blocked until the new tokens are present in the runtime environment. | The order says re-run after Railway env vars are updated; no destructive or secret-printing workaround is acceptable. | Blocker only; no outbound writes attempted. |
| 2026-07-04T10:41:22-04:00 | core | Pushed only clean `build/core` and `build/nexi` branches to origin; kept `archive/pre-bible-main` local only. | Reachable history and credential filename probes were clean before push; the archive branch remains quarantined and must never be pushed. | `receipts/m0/push-railway-status.txt` |
| 2026-07-04T10:41:22-04:00 | core | Railway deploy is blocked because the available `NexTeam-Studio` Railway target lists only `production`, with no linked staging environment/service. | The hard limit says live Railway v1 stays untouched; deploying to a production-only target would violate that. | `receipts/m0/push-railway-status.txt` |
| 2026-07-04T11:00:08-04:00 | nexi | M1 production path now uses the single Nexi Anthropic gateway/tool loop; deterministic local behavior is only available behind explicit `NEXI_LOCAL_FAKE_GATEWAY=true`. | This preserves F6 in production while allowing no-outbound local receipts under the autonomy hard limits. | `packages/nexi/src/gateway.ts`, `apps/server/src/nexi/nexiService.ts`, `receipts/m1/local-transcripts.json` |
| 2026-07-04T11:00:08-04:00 | nexi | Conversations, failureLog, SiteJobBlueprints, and usageLog parse through zod schemas before memory or Firestore persistence. | E-1/E-2 require trusted boundaries and tenant-scoped documents; memory remains a local fallback when `FIREBASE_SERVICE_ACCOUNT` is absent. | `apps/server/src/nexi/nexiRepository.ts`, `packages/core/src/schemas.ts`, `receipts/m1/verify.txt` |
| 2026-07-04T11:00:08-04:00 | nexi | In-app Browser screenshot capture was blocked by a browser plugin runtime path error, so the phone UI receipt was captured with the repo Playwright dependency. | The UI receipt was still required; fallback avoided claiming unavailable Browser verification. | `receipts/m1/mobile-ui.png` |
| 2026-07-04T11:00:08-04:00 | nexi | M1 live receipts remain blocked until staging Railway and runtime secrets are available. | The Bible requires live real-data transcripts, live cache-read usageLog, and phone recording; current Railway listing exposes production only, and this shell has no Jobber/CompanyCam env variables. | `receipts/m0/push-railway-status.txt`, `receipts/m1/verify.txt` |
| 2026-07-04T12:53:20-04:00 | core | Credential incident CLOSED. | Chris confirmed the old WordPress application password was revoked and the editor password changed; new WordPress publish credentials live only in Railway env vars for M5, and no WordPress-shaped code, tests, API calls, files, commits, or chat are allowed before M5 opens. | This file |
| 2026-07-04T12:53:20-04:00 | core | Created Railway `staging` by duplicating `production`, deployed `build/core`, and added non-secret `GIT_SHA` for CLI-upload SHA receipts. | Railway CLI upload does not provide `RAILWAY_GIT_COMMIT_SHA`; `/api/version` needs a deterministic non-secret SHA source for E-9. | `receipts/m0/staging-version-health.json` |
| 2026-07-04T12:53:20-04:00 | core | Firebase owner UID rule remains blocked pending confirmation. | Admin SDK exact lookup for `chris1bata@gmail.com` returned user-not-found; the only Chris/Bata-like Auth user found was `chris@aquatraceleak.com` / `USX9Lc0M6mdRU7QxrMNjJhiV2p42`, and the order explicitly requested confirmation before rules are finished. | Admin SDK lookup output in chat; no secret values printed. |
| 2026-07-04T12:53:20-04:00 | core | Fresh staging Jobber and CompanyCam smoke tests passed read-only. | Rotated provider credentials are present in staging env and both rails returned green raw responses without destructive writes. | `receipts/m0/staging-smoke-m0.txt` |
| 2026-07-04T14:20:00-04:00 | core | Firestore admin rules are wired to confirmed Chris admin UID `USX9Lc0M6mdRU7QxrMNjJhiV2p42`. | Chris confirmed `chris@aquatraceleak.com` / UID `USX9Lc0M6mdRU7QxrMNjJhiV2p42` in-channel; the prior placeholder/uncertainty blocker is cleared. | `firestore.rules`, `receipts/m0/firestore-admin-rules-uid.txt` |
| 2026-07-03T23:59:00-04:00 | core | Reporting format follows the user's overnight order: daily line per worktree plus BUILDSTATE receipts. | This is the most conservative resolution of the Bible's report-format conflict because it preserves both receipt tracking and the required one-line daily report. | This file |
| 2026-07-03T23:59:00-04:00 | core | Tenant root documents are treated as the only E-2 exception; all tenant-owned child docs must carry `tenantId`. | Part 4 defines `Tenant.id` but not `Tenant.tenantId`; adding a duplicate field to tenant roots would conflict with the exact contract. | `packages/core/src/schemas.ts` |
| 2026-07-04T00:10:00-04:00 | core | New M0 verification targets `apps/`, `packages/`, `tests/`, and `scripts/check-*`; legacy root `src/` is preserved but outside the new CI-law target until it is ported. | This preserves v1 locally while allowing the Bible contracts to become enforceable on the new product surface first. | `eslint.config.js`, `scripts/check-provider-imports.mjs` |
| 2026-07-04T00:10:00-04:00 | core | Firestore admin gate uses Chris email plus existing `platform_operator` claim because Chris's exact Firebase UID is not present in local spec/context. | Conservative no-client-write posture is preserved; owner can replace the email/claim gate with exact UID in the morning. | `firestore.rules` |
| 2026-07-04T00:20:00-04:00 | core | `/api/health` checks Jobber and CompanyCam with read-only calls when configured; Anthropic health is configuration-only to avoid token spend. | Overnight hard limit forbids spending; a live Anthropic message would create cost just to check health. | `apps/server/src/health.ts`, `receipts/m0/local-version-health.json` |
| 2026-07-04T00:20:00-04:00 | core | Local Node v24 was used for verification even though the target engine is Node 20. | The build manifest and Dockerfile target Node 20; the desktop runtime currently reports Node v24 and npm warned about the engine mismatch. | `receipts/m0/verify.txt` |
| 2026-07-04T00:20:00-04:00 | core | Did not run `npm audit fix --force`. | `npm install` reported 17 vulnerabilities, but force-fixing during M0 could introduce unrelated dependency churn; record for owner review. | npm install output |
| 2026-07-04T00:20:00-04:00 | core | Live `/api/version` SHA-match receipt is blocked until push plus a separate Railway environment. | GitHub CLI is unavailable, pushing waits for morning, and the live v1 Railway deployment must remain untouched. | `BUILDSTATE.md` |
| 2026-07-04T00:35:00-04:00 | nexi | M1 route work is committed only as a blocked local skeleton, not as a completion claim. | Overnight no-spend and no-live-deploy limits conflict with the Bible's M1 live Claude/tool-loop, usage-cache, phone UI, and live transcript receipts. | receipts/m1/local-routes.json |
| 2026-07-04T13:55:00-04:00 | nexi | Firestore Nexi reads sort in memory instead of requiring composite indexes for M1 receipt paths. | Staging failed live message calls on missing `tenantId + createdAt` Firestore indexes; in-memory sorting is conservative for low-volume receipt/read-side flows and avoids manual index creation as a hidden deploy dependency. | `apps/server/src/nexi/nexiRepository.ts`, `receipts/m1/staging-live-transcripts.json` |
| 2026-07-04T13:55:00-04:00 | nexi | `conversationId` is now persisted and used to scope Nexi history. | Tenant-wide history polluted independent live transcript runs and caused tool-use flakiness; scoped conversations keep persistence without cross-contamination. | `apps/server/src/nexi/nexiRoutes.ts`, `apps/server/src/nexi/nexiService.ts`, `receipts/m1/staging-live-transcripts.json` |
| 2026-07-04T13:55:00-04:00 | nexi | Obvious M1 Job Desk intents deterministically run the read-only tool before one Anthropic wording call. | Live model-side tool selection was flaky for required schedule/photo/blueprint acceptance prompts; deterministic read-only tool preloading preserves sources, usageLog, and no outbound writes. | `packages/nexi/src/gateway.ts`, `receipts/m1/staging-live-transcripts.json` |
| 2026-07-04T13:55:00-04:00 | nexi | Cache-read acceptance is proven with a separate non-deterministic readiness prompt. | Deterministic tool preloading omits tool schemas to keep one model call stable, so A/B/C transcript usage can show zero cache reads; the dedicated two-call cache receipt proves persisted `cacheReadInputTokens > 0` without changing acceptance answers. | `receipts/m1/staging-cache-proof.json`, `receipts/m1/staging-usage-log.json` |
| 2026-07-04T13:55:00-04:00 | nexi | Staging serves the built Vite mobile chat UI from `apps/web/dist`. | `/` initially returned API JSON, which blocked the phone-viewport UI receipt; the server now serves static web assets using an absolute path from the compiled server module. | `apps/server/src/server.ts`, `receipts/m1/staging-phone-chat.webm` |
| 2026-07-04T16:57:04-04:00 | nexi | Parked requested email capability as M6-only. | Owner requested email capability but explicitly ordered no early build; M1 failureLog may collect the need, but no email code/API calls happen before M6 opens. | `PARKED.md` |
| 2026-07-04T18:25:41-04:00 | nexi | Added Firebase operator sign-in to the M1 chat UI and server-side ID token enforcement for Nexi routes. | The owner identified no sign-in as an M1 spec gap; staging has Firebase Admin and web config variables, so the conservative fix is email/password sign-in plus UID/email/claim authorization. | `apps/web/src/main.tsx`, `apps/server/src/nexi/nexiRoutes.ts`, `receipts/m1/firebase-auth-verify.txt` |
| 2026-07-04T18:25:41-04:00 | nexi | Stripe remains blocked because Railway API-visible staging variables contain no `STRIPE_SECRET_KEY`. | A CLI redeploy succeeded, but sanitized variable scans still show no Stripe variables; a UI-only unsaved value cannot be read, deployed, or used safely by Codex. | `receipts/m2/stripe-staging-variable-scan.json` |
| 2026-07-04T18:25:41-04:00 | nexi | No live Stripe key was found in the redacted local code/config scan. | The scan reports filenames/counts only and found zero `sk_live_` matches after removing a fake unit-test prefix in the CRM worktree. | `receipts/security/sk-live-scan.json` |
| 2026-07-04T18:33:00-04:00 | nexi | `/api/version` now prefers explicit `GIT_SHA` over Railway's retained `RAILWAY_GIT_COMMIT_SHA` for file-upload deploys. | Railway `up` deploys preserved an older Git SHA even though the uploaded app was new; explicit `GIT_SHA` gives staging a verifiable deploy SHA without touching production. | `apps/server/src/buildInfo.ts` |
| 2026-07-04T18:37:00-04:00 | nexi | Firebase web config falls back to `/api/public/runtime-config` at runtime. | Railway file-upload builds did not inject `VITE_FIREBASE_*` into the Vite bundle even though staging variables exist; runtime public config keeps auth working without hardcoding Firebase values. | `apps/server/src/server.ts`, `apps/web/src/main.tsx` |
| 2026-07-04T15:37:00-04:00 | crm | Removed CRM dependency shims and regenerated M2 dry-run receipts against a real local `node_modules`. | The previous M2 receipts were invalid under the owner shim rule because third-party packages junctioned to Nexi; this commit records the replacement receipt set. | `receipts/m2/dependency-install-proof.txt`, `receipts/m2/jobber-import-dry-run.json` |
| 2026-07-04T15:45:00-04:00 | crm | Seeded VGB catalog IDs `VGB-001` through `VGB-072` with zero default prices and quote-time price overrides. | The Bible requires a VGB catalog but does not provide owner-approved pricing; zero defaults prevent invented pricing while allowing approval-gated quote drafts with explicit inputs. | `packages/industry-packs/src/poolLeakVgbCatalog.ts` |
| 2026-07-04T15:45:00-04:00 | crm | Parked Stripe live/test execution until Stripe keys are present. | The M2 money path cannot be receipted without `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET`; no checkout/session calls are made without those env vars. | M2 blocker for final Stripe receipt |
| 2026-07-04T16:35:00-04:00 | crm | Superseded the zero-price VGB seed with the read-only Jobber Products & Services pull. | Chris ordered Jobber to be authoritative; `ProductOrService.defaultUnitCost` is the accessible unit-price field under current permissions, while `lastQuoteLineItem` is hidden by Jobber permissions. | `receipts/m2/jobber-catalog-pull.json`, `packages/industry-packs/src/poolLeakVgbCatalog.ts` |
| 2026-07-04T16:35:00-04:00 | crm | Stripe test receipt remains blocked because staging has no `STRIPE_SECRET_KEY`. | The safe staging env check returned configured=false/mode=missing; live-mode keys remain permanently blocked without approval. | Staging key-mode check in chat; no secret value printed. |
| 2026-07-04T16:57:04-04:00 | crm | Added the M2 Stripe code path behind test-mode-only guards, but did not call Stripe because staging still reports no `STRIPE_SECRET_KEY`. | Checkout and webhook code can now be verified locally; live webhook creation and card receipt require the staging key to be visible first. | `apps/server/src/crm/stripe.ts`, `scripts/stripe-staging-webhook-setup.ts` |
| 2026-07-04T16:57:04-04:00 | crm | Parked requested email capability as M6-only. | Owner requested email capability but explicitly ordered no early build; all email-shaped work stays parked until M6 opens. | `PARKED.md` |
| 2026-07-04T18:25:41-04:00 | crm | Removed a fake `sk_live_` unit-test literal so live-key scans only catch real contiguous values. | The test still proves live-mode refusal by constructing the prefix at runtime; the repository scan now has zero `sk_live_` matches. | `apps/server/test/crm-read-side.test.mjs` |
| 2026-07-04T20:50:00-04:00 | crm | Added `Property.externalIds.jobber` and idempotent native upserts for Jobber import writes. | The Bible requires native imports to preserve Jobber external IDs one-to-one; clients/jobs already did, properties did not. | `packages/core/src/schemas.ts`, `packages/providers/src/native/NativeAdapter.ts`, `apps/server/src/crm/nativeRepository.ts` |
| 2026-07-04T20:50:00-04:00 | crm | Jobber full import remains dry-run complete but native-write blocked. | Dry-run returned 625 clients, 625 jobs, and 547 properties with `jobberWrites:false`; `--write-native` stopped before writes because Railway staging does not expose `FIREBASE_SERVICE_ACCOUNT` to the process. | `receipts/m2/jobber-full-import-dry-run.json`, `receipts/m2/jobber-native-import-write.json`, `receipts/m2/staging-env-presence-check.json` |
| 2026-07-04T20:50:00-04:00 | crm | Created the Stripe test webhook and stored `STRIPE_WEBHOOK_SECRET` without printing secret values. | `STRIPE_SECRET_KEY` is now present in staging; the script cleaned the prior failed test endpoint, created a new test endpoint, and used stdin to store the signing secret. | `receipts/m2/stripe-webhook-setup.json` |
| 2026-07-04T20:50:00-04:00 | crm | M2 live payment receipt is blocked until CRM routes are deployed through an integration build that preserves M1. | Current staging SHA is the Nexi deploy and `/api/crm/clients` returns 404, so a live Stripe checkout would not have the required CRM endpoints. | `receipts/m2/staging-crm-route-probe.json` |
| 2026-07-04T15:37:00-04:00 | fielddocs | Removed FieldDocs dependency shims and regenerated M4 dry-run receipts against a real local `node_modules`. | The previous M4 receipts were invalid under the owner shim rule because third-party packages junctioned to Nexi; this commit records the replacement receipt set. | `receipts/m4/dependency-install-proof.txt`, `receipts/m4/companycam-import-dry-run.json` |
| 2026-07-04T16:05:00-04:00 | fielddocs | Kept live vision analysis parked behind `FIELD_DOCS_VISION_ENABLED` placeholder behavior. | The Bible requires vision captions, but the active hard limit forbids spend; upload/search/report rails are built, while paid live Sonnet vision receipts wait for an approved spend/test run. | `apps/server/src/fielddocs/visionPipeline.ts` |
| 2026-07-04T16:05:00-04:00 | fielddocs | Native upload storage refs use `native://tenants/{tenantId}/...` and thumbnails are internal refs only. | This preserves F5: no CompanyCam or third-party authenticated media URL can reach browser/mobile clients from Field Docs. | `apps/server/src/fielddocs/uploadService.ts` |
| 2026-07-04T16:40:00-04:00 | fielddocs | Ran one paid live vision receipt against a real CompanyCam photo under the approved $5 cap. | Actual estimated spend was $0.013020; no outbound report delivery or client notification was attempted. | `receipts/m4/live-vision-receipt.json` |
| 2026-07-04T20:30:00-04:00 | fielddocs | Treated M4 "posted report" as a native `fieldReports` record plus PDF receipt, not external delivery. | The active hard limits still forbid outbound sends without explicit approval; this proves the FieldDocs report handoff point while keeping delivery parked. | `receipts/m4/native-report-post-receipt.json`, `receipts/m4/native-field-report.pdf` |
| 2026-07-04T20:30:00-04:00 | fielddocs | Continued M2/M4 before opening Wave 2. | User standing order says finish M2 and M4 to full Bible spec first; Wave 2 worktrees M3, M5, M12a, and M13 open only after both merge. | This file |
| 2026-07-04T21:36:00-04:00 | crm | M2 Stripe payment receipt treats the Stripe webhook-backed Firestore `invoice.paid` event as authoritative while still recording Checkout redirect. | Stripe Checkout UI shape can vary by account/session, so the conservative proof is the signed live webhook updating native state; the receipt remains test-mode only and prints no secrets. | `receipts/m2/stripe-payment-receipt.json`, `receipts/m2/staging-env-confirm-after-integration.json` |
| 2026-07-04T21:55:00-04:00 | integration | Merged `build/integration-m2-m4` into `main` with unrelated histories allowed and resolved add/add conflicts to the receipted integration side. | The Bible supersedes prior specs, and the integration branch contains the accepted M0/M1/M2/M4 receipts; unrelated main-only files were preserved where Git had no conflict. | Merge commit `5050cda83978acbae223531893ec5361f2acff9b`, `receipts/main/staging-main-version.json` |
| 2026-07-04T21:55:00-04:00 | wave2 | M3/M5/M12a/M13 worktrees are open, but outbound sends/publishing remain parked behind explicit approval. | Owner reconfirmed no exceptions during Wave 2; content and campaign work may build only to ApprovalQueue artifacts until approval. | `BUILDSTATE.md`, `PARKED.md` |
| 2026-07-04T22:43:00-04:00 | nexi | M1 source gating now exempts meta/tool questions and user feedback while still blocking unsourced business/job facts. | Trial Day 1 showed F7 overfiring on "what sources do you use", correction follow-ups, and UI bug reports; the gate now receives the user prompt before deciding whether source enforcement applies. | `packages/nexi/src/sourceCheck.ts`, `receipts/m1/trial-day1-p1-fix-receipt.json` |
| 2026-07-04T22:43:00-04:00 | nexi | SiteJobBlueprint field lookup is keyed by requested entity/job and returns no source/value on mismatch. | Deborah Justice gallons incorrectly reused the Camp Mikell `101000` blueprint; the conservative fix rejects cross-entity blueprint matches instead of guessing. | `apps/server/src/nexi/nexiTools.ts`, `receipts/m1/trial-day1-p1-live-staging-receipt.json` |
| 2026-07-04T22:43:00-04:00 | nexi | User "wrong/incorrect" replies now write `failureLog.reason=user_flagged_incorrect` linked to the prior answer. | Trial Day 1 corrections were invisible to failureLog unless Nexi recognized its own failure; user flags are now first-class intake. | `apps/server/src/nexi/nexiService.ts`, `receipts/m1/trial-day1-p1-live-staging-receipt.json` |
| 2026-07-04T22:43:00-04:00 | nexi | Staging P1 deploy used pinned code commit `eabcb436e9b8f3296f632bd154acad2d457657a4` plus explicit non-secret `GIT_SHA`; receipt commits remained separate. | The live SHA-match must prove the code commit, not the later receipts-only commits. | `receipts/m1/trial-day1-p1-staging-deploy.txt`, `receipts/m1/trial-day1-p1-live-staging-receipt.json` |
| 2026-07-05T10:49:00-04:00 | nexi | CompanyCam report reading stays server-side: document metadata can be returned, but PDF URLs are fetched only inside the provider/server path and never sent to browser/mobile clients. | P1-D requires report findings from CompanyCam PDFs while preserving F5 media/document boundary rules. | `packages/providers/src/companycam/CompanyCamAdapter.ts`, `apps/server/src/nexi/reportDocuments.ts`, `receipts/m1/p1d-companycam-docs-live-staging.json` |
| 2026-07-05T10:49:00-04:00 | nexi | Deborah Justice's CompanyCam report was ingested into native SiteJobBlueprint `site_job_companycam_18218446` with `poolGallons=37602`. | Trial Day 1 proved Camp Mikell-only blueprint data was unsafe; client report-derived fields now seed native lookups before user trial restarts. | `receipts/m1/companycam-report-ingest-deborah-justice.json` |
| 2026-07-05T11:12:00-04:00 | nexi | Schedule answers keep raw Jobber `startAt`/`endAt` while adding tenant-local `schedule.localSummary` for Nexi wording. | Trial P2-E requires "what's on Monday" to use scheduled visit dates; local summaries prevent the assistant from treating Eastern all-day Jobber windows as UTC placeholders. | `receipts/m1/p2e-schedule-live-staging.json` |
| 2026-07-05T11:26:00-04:00 | nexi | Cross-rail job questions now treat a checked-but-empty rail as evidence, not a fatal error. | Deborah Justice exists in CompanyCam but has no matching Jobber job; P2-F requires both rails be checked, so the answer should use CompanyCam while explicitly saying Jobber returned no match. | `receipts/m1/p2f-p2g-live-staging.json` |
| 2026-07-05T11:26:00-04:00 | nexi | Photo UI save/open actions stay on the `/api/media/:id` proxy and never expose CompanyCam URLs. | P2-G requires full-size and savable photos while preserving F5; the browser gets same-origin image/download endpoints only. | `receipts/m1/p2f-p2g-live-staging.json` |
| 2026-07-05T12:11:00-04:00 | nexi | Photo prompts now parse trailing-entity wording such as "show me the Deborah Justice photos" as `projectQuery=Deborah Justice`. | Trial restart found that the older parser handled "photos for X" but not "X photos"; exact live retest now hits CompanyCam `getPhotos` and returns sourced photos instead of the no-source stonewall. | `packages/nexi/src/gateway.ts`, `receipts/m1/p2-photo-context-reuse-format-live-staging.json` |
| 2026-07-05T12:11:00-04:00 | nexi | Schedule follow-ups use the conversation's prior date window and can reuse saved deterministic tool traces when the user did not provide a fresh target. | Trial P2 showed ETA follow-ups drifting to July 5 after a July 6 schedule question; persisted `toolRuns` plus context-window parsing keep the follow-up grounded and reduce redundant calls. | `packages/core/src/schemas.ts`, `apps/server/src/nexi/nexiService.ts`, `packages/nexi/src/gateway.ts`, `receipts/m1/p2-photo-context-reuse-format-verify.txt` |
| 2026-07-05T12:11:00-04:00 | nexi | The "only what was asked" SOUL rule is enforced in code by stripping trailing next-step/menu sentences before source enforcement returns the answer. | Prompt-only instruction still allowed "Want me..." and "Let me know if..." tails in live answers; the server now removes those endings while preserving factual sourced content. | `docs/internal/tmnt/nexi/NEXI_V1_JOB_DESK_SOUL.md`, `packages/nexi/src/gateway.ts`, `receipts/m1/p2-photo-context-reuse-format-live-staging.json` |
| 2026-07-05T12:11:00-04:00 | comms | M6-LITE Email Rail is pulled into the current window after M1 trial fixes, but outbound email remains ApprovalQueue-only. | Owner added read-only Gmail tools for two existing mailboxes plus send from a third dedicated mailbox; the two read-only adapters must be structurally send-incapable, tokens stay in Railway env vars only, and email receipts/logs must avoid content bodies. | `BUILDSTATE.md`, `PARKED.md` |
| 2026-07-05T12:45:00-04:00 | comms | `build/comms-lite` is based on `build/nexi` instead of stale `main` so staging can preserve the receipted M1 trial fixes while the email rail is tested. | M6-LITE was pulled into the current window before M1 trial fixes merged to main; basing on `build/nexi` avoids redeploying a branch that regresses the active field trial. | `receipts/m1/p2-photo-context-reuse-format-live-staging.json` |
| 2026-07-05T12:45:00-04:00 | comms | Gmail read and send are separate adapter classes: read-only mailboxes use `GmailReadOnlyAdapter` with no `sendEmail`; the dedicated sender uses `GmailSendAdapter` with no read/search methods. | The owner required structural send incapability for the two existing mailboxes, not merely policy checks. | `packages/providers/src/gmail/GmailAdapter.ts`, `apps/server/test/comms-lite.test.mjs`, `receipts/m6-lite-verify.txt` |
| 2026-07-05T12:45:00-04:00 | comms | Nexi email send requests create `ApprovalQueue` email artifacts; actual Gmail send is only reachable from `executeApproved` and only for the dedicated send mailbox alias. | This preserves E-6: no direct/auto outbound sends. A queued draft may be approved, then executed through the ApprovalQueue executor. | `apps/server/src/comms/nexiTools.ts`, `apps/server/src/comms/approvalExecutor.ts`, `receipts/m6-lite-verify.txt` |
| 2026-07-05T12:45:00-04:00 | comms | Email tool traces are redacted before conversation persistence. | M6-LITE allows Nexi to summarize email metadata/content, but email body/snippet fields must not become durable logs through the M1 cached `toolRuns` path. | `apps/server/src/nexi/nexiService.ts` |
| 2026-07-05T12:45:00-04:00 | comms | Live M6-LITE receipts are blocked on owner OAuth consent and third mailbox creation. | No Gmail refresh tokens or mailbox credentials exist in Railway staging yet, and tokens must not be written to repo files or chat. | `receipts/m6-lite-oauth-readiness.txt` |
| 2026-07-05T14:18:40-04:00 | comms | The original consumer Gmail mailbox is mandatory for M6-LITE read access, so the OAuth app must support external Google accounts. | `aquatraceleak@gmail.com` contains legacy Jobber/account/subscription traffic; the current Internal-only OAuth app blocks it with `org_internal`, so swapping the mailbox out is not acceptable. | Google Auth Platform Audience configuration |
| 2026-07-05T14:32:46-04:00 | comms | Discard the first External Google OAuth client because its client secret was visible in an owner-shared setup screenshot. | Per F9/no-secrets discipline, the exposed client must not be used; recreate a fresh OAuth client, download its JSON locally, and avoid posting screenshots that show the secret. | Google Auth Platform Clients |
| 2026-07-05T14:49:48-04:00 | comms | `nexi@aquatraceleak.com` is a dedicated read/send mailbox, not send-only. | Owner clarified Nexi should read her own account while the two existing mailboxes remain structurally send-incapable; outbound sends from Nexi still execute only through ApprovalQueue. | `apps/server/src/comms/gmailRegistry.ts`, Railway `GMAIL_SEND_MAILBOX_READ_ENABLED=true` |
| 2026-07-05T15:19:56-04:00 | comms | Email rails are bound to a single tenant at construction and reject mismatched tenant contexts before adapter access. | Owner required per-tenant architecture, not convention; `CommsRail.tenantId` is enforced in Nexi read/draft tools and ApprovalQueue execution before any Gmail adapter call. | `apps/server/src/comms/gmailRegistry.ts`, `apps/server/src/comms/nexiTools.ts`, `apps/server/src/comms/approvalExecutor.ts`, `receipts/m6-lite-tenant-isolation.txt` |
| 2026-07-05T17:40:30-04:00 | fielddocs | Report extraction now treats Aquatrace Job Reports, Moasure exports, and evap PDFs as a reusable date-linked report set, with CompanyCam filenames treated as unreliable metadata. | Owner locked the field dictionary and rules: Moasure links by visit date, evap PDFs override checklist evap index, loss fractions parse to decimals, gallons/inch is always `sq ft * 0.625`, defects without hard water loss remain PASS, and generic/orphan docs are parked for review instead of guessed. Real sample PDFs had no parsed R3 conflict, so the local receipt includes a clearly labeled synthetic R3 conflict fixture without altering source data. | `apps/server/src/fielddocs/reportExtraction.ts`, `receipts/m4/report-extraction-schema-receipt.json` |
| 2026-07-05T17:40:30-04:00 | fielddocs | Vision ingestion policy is lazy by default for the Aquatrace back-catalog. | New photos are captioned on upload; legacy photos are captioned opportunistically when an old client gets a new job, on first query, or in approved active/recurring batches. Low-confidence legacy items should produce an end-of-day human confirmation prompt instead of full-catalog paid captioning. | `PARKED.md`, `apps/server/src/fielddocs/reportExtraction.ts` |
| 2026-07-06T00:00:00-04:00 | security | Local Railway CLI credential values appeared in tool output during staging environment inspection; no secret values were copied into code, receipts, commits, or chat. | Treat the local CLI session as exposed and rotate/re-auth Railway CLI access before any push from this machine. | Local Railway CLI config; no repo file |
| 2026-07-06T14:45:00-04:00 | security | Railway CLI incident closeout: the exposed credential class was the local Railway CLI access token and refresh token stored under the user auth cache, not an app env var. | Exposure location was Codex tool output only; repo scans and staging deploy logs did not contain token values. `railway logout` cleared the local token values and `railway whoami` now returns unauthorized. A fresh Railway login or dashboard-created project token is required for future CLI actions. | `C:\Users\Peyto\.railway\config.json`, `ACCOUNTS.md` |
| 2026-07-06T16:36:37-04:00 | security | Railway staging auth now uses a dashboard-created project token captured through a masked PowerShell prompt into a user-local DPAPI vault. | The token must never be pasted into chat, repo files, receipts, or Railway app variables; Railway commands run only through the wrapper that injects `RAILWAY_TOKEN` into a child process and redacts token-shaped output. | `scripts/security/capture-railway-staging-token.ps1`, `scripts/security/invoke-railway-staging.ps1`, `%APPDATA%\NexTeam-Studio\secrets\railway-staging.dpapi` |
| 2026-07-07T01:44:00-04:00 | evaporation | OpenWeather credential incident opened: the legacy Aquatrace calculator has a hardcoded OpenWeather key, and a formula-inspection command surfaced that line in local tool output. | The key value was not copied into repo files, receipts, commits, or final chat, and it was not reused for staging. The old key should be rotated and replaced by a Railway staging env var before the live Item 7 receipt runs. | `C:\Users\Peyto\aquatrace-app\assets\leak-calculator.html`, local tool output only |
| 2026-07-07T10:08:00-04:00 | nexi | Watch item: the no-source stonewall has recurred across five trigger phrases and must be treated as its own root-cause class if it appears again. | Next recurrence must audit every code path that can produce the missing-source/missing-data wording and unify them behind one policy; do not patch only the newest phrase. This is logged per owner approval and is not blocking Item 7. | `packages/nexi/src/sourceCheck.ts`, `packages/nexi/src/gateway.ts`, `apps/server/src/nexi/nexiService.ts` |
| 2026-07-07T10:09:00-04:00 | evaporation | OpenWeather credential incident closed: the exposed legacy key is rotated/dead and staging now uses `OPENWEATHER_API_KEY` from Railway env vars only. | Item 7 may run live against staging; no OpenWeather key value may appear in repo files, receipts, logs, or chat. | Railway staging env var location only |
| 2026-07-08T08:31:00-04:00 | campaigns | M6 bulk/list execution remains structurally blocked even after ApprovalQueue drafting until SPF, DKIM, DMARC, a tenant physical mailing address, and explicit owner approval are all present. | Campaigns can now build audiences, render configurable templates, queue approval artifacts, track opens/clicks/unsubscribes, honor suppression, and queue transactional report/review messages, but no bulk/list send may execute without legal/domain readiness. | `apps/server/src/campaigns/compliance.ts`, `receipts/m6/campaigns-live-receipt-20260708-current.json` |
| 2026-07-08T08:31:00-04:00 | nexi | The full live Nexi regression wall was run in deterministic chunks after a single 174-case runner hung without writing a receipt. | Chunked live receipts avoid duplicate long-running Anthropic work and make any future stuck case visible by fixture prefix; aggregate receipt proves the complete wall still totals `174/174`. | `receipts/m1/nexi-regression-wall-live-20260708-m6-aggregate.json` |
| 2026-07-08T11:31:00-04:00 | mobile | M11 is code- and staging-receipted but not merge-complete until a physical phone proves the airplane-mode flow. | The Expo shell, mobile API rail, local offline receipt, live API receipt, and live wall are green; however the Bible explicitly requires a real-device airplane-mode job proof, and this Codex runtime has no connected phone to record that final receipt. | `receipts/m11/mobile-offline-first-receipt-current.json`, `receipts/m11/mobile-live-api-receipt-current.json`, `receipts/m1/nexi-regression-wall-live-20260708-m11-aggregate.json` |
| 2026-07-08T13:36:00-04:00 | website | M8 staging launch is Railway-internal only until owner completes Cloudflare/custom-domain setup. | The generated Aquatrace site, lead intake, CRM visibility, and owner notification queue are live on staging; public custom domain, SSL, and DNS cutover are intentionally not attempted from Codex. | `receipts/m8/website-live-receipt-current.json`, `receipts/m8/website-mobile-lead-form-current.png` |
| 2026-07-08T13:36:00-04:00 | nexi | Live proof harnesses must force the requested tenant into minted proof-session claims instead of inheriting `FIREBASE_DEFAULT_TENANT_ID`. | M8 receipt initially failed because Railway's default proof tenant did not match `aquatrace`; the receipt and regression-wall runners now make tenant context explicit so future wall runs are not dependent on ambient environment defaults. | `scripts/m8-website-live-receipt.mjs`, `scripts/run-nexi-regression-wall.mjs`, `receipts/m1/nexi-regression-wall-live-20260708-m8-aggregate.json` |
| 2026-07-08T20:05:00-04:00 | campaigns | M6 is done for build-to-ApprovalQueue; bulk/list sending remains a parked external send boundary. | The campaign engine has live receipts for AccessContext-gated generation, templates, XState sequence queueing, compliance injection, suppression, tracking, transactional approval artifacts, and ApprovalQueue-only behavior. Public SPF/DKIM/DMARC presence does not authorize sends by itself; execution still requires explicit staging env confirmations, a tenant physical mailing address, and owner approval. | `receipts/phase0/m6-campaigns-blocker-resolution-20260708.json` |
| 2026-07-08T20:26:00-04:00 | phase0 | `createClient` and content queue now pass the Part 9 user-facing reality gate on live staging. | The live receipt proves owner-facing Nexi prompts can queue a new client, show the approval item, execute the client after approval, show pending content drafts, approve content from chat, and reject content through the web-backed route while keeping publishing deferred. | `receipts/phase0/phase0-reality-live-receipt-current.json` |
| 2026-07-08T21:37:00-04:00 | nexi | Classes A, C, and D are CLOSED after the required unrelated-deploy soak. | The full live wall was rerun against staging after Phase 0 reality-gate work and passed `200/200`; the only adjustment was correcting the generic "Who owes us money" fixture to the previously verified Class D capability-gap behavior while keeping specific payment questions cross-rail. | `receipts/m1/nexi-regression-wall-live-20260708-phase0-soak-aggregate.json` |
| 2026-07-08T22:10:00-04:00 | fielddocs | Vision survey uses conservative metadata review until a real owner-selected CompanyCam batch is run; `INSUFFICIENT` is a valid result, not a failure. | The owner explicitly made guessing the failure mode. The MVP stores structured tags and low-confidence prompts, but it does not claim full visual certainty or a live batch receipt until the selected folder is reviewed. | `apps/server/src/fielddocs/visionSurvey.ts`, `receipts/m4/vision-survey-local-receipt-20260708.json` |
| 2026-07-08T22:10:00-04:00 | fielddocs | Any vision survey batch estimated over the $5 cap blocks before review writes. | Paid vision and large legacy photo runs are cost-controlled by owner approval; the local MVP estimates spend and refuses over-cap batches instead of silently running them. | `apps/server/src/fielddocs/visionSurvey.ts` |
| 2026-07-08T22:45:00-04:00 | context | Distance questions default to the Aquatrace home base and use Google Maps only when the staging key is configured. | Owner specified "from 102 Kate Lane"; the tool reads configurable home-base env vars first, then falls back to the known Aquatrace 102 Kate Lane address. Local tests use a stub/heuristic, while live receipts must prove the Google Maps-backed path. | `apps/server/src/context/nexiTools.ts`, `packages/nexi/src/gateway.ts`, `receipts/phase-c/distance-tool-local-receipt-20260708.json` |
| 2026-07-08T22:45:00-04:00 | platform | Multi-user job links store only token hashes; raw one-time tokens are withheld from route responses unless an explicit admin/test request asks for them. | Job-scoped links are credentials. Keeping hashes in durable records and redacting list responses prevents receipts/backups from becoming credential stores while preserving a future delivery path. | `apps/server/src/platform/accessManagement.ts`, `apps/server/src/platform/routes.ts` |
| 2026-07-08T22:45:00-04:00 | platform | Aquatrace seed users start as Chris OWNER, Catherine TECHNICIAN, and Logan TECHNICIAN. | This matches the owner-confirmed amendment and avoids assuming office/admin privileges for field techs until the owner changes the role. | `apps/server/src/platform/repository.ts` |
| 2026-07-08T23:30:00-04:00 | platform | Self-repair rail uses tenant export plus safe-repair logging only; it does not mutate code, SOUL, schemas, deploys, or outbound sends. | The nightly diagnosis rail should multiply trial auditing without becoming an unsupervised builder or sender. Morning reports are queued as ApprovalQueue email artifacts with no selfRepair send executor until explicitly approved and wired. | `receipts/m13/self-repair-local-receipt-20260708.json` |
| 2026-07-09T01:55:00-04:00 | platform | Self-repair analysis is deterministic-first with an optional Anthropic reviewer, never Anthropic-only. | Known Part 9 classes must be structurally caught without provider drift, while the live Anthropic pass can spot unflagged issues and logs `self_repair_analysis` cost to usageLog. Safe repairs remain limited to metadata/wall candidates and never code, SOUL, deploys, schemas, or outbound sends. | `apps/server/src/selfrepair/analyzer.ts`, `apps/server/src/selfrepair/anthropicAnalyzer.ts`, `receipts/m13/self-repair-local-receipt-20260708.json` |
| 2026-07-09T02:55:00-04:00 | integration | Staging live chat and Anthropic self-repair review are blocked by Anthropic returning `credit balance is too low`. | The integration branch deploys to staging at SHA `3bb3d5b0ca56de3d9b94bb82941a3169d9c60c01` and `/api/health` is green, but `/api/nexi/message` returns the billing error before live prompt receipts can complete; self-repair falls back to deterministic mode without Anthropic cost proof. | `receipts/integration/nightly-integration-live-receipt-20260709.json` |
| 2026-07-09T04:31:00-04:00 | security | Railway variable listing is no longer an approved diagnostic path unless passed through expanded redaction. | `railway variables` printed secret-looking staging variable values in internal tool output while diagnosing a SHA mismatch. No values were committed to git or repeated in user-facing chat, but the wrapper now redacts API-key, token, password, refresh-token, webhook-secret, and known-provider variable rows before output. | `scripts/security/invoke-railway-staging.ps1`; rotate affected provider keys if policy treats internal tool output as exposed. |
| 2026-07-09T09:00:00-04:00 | security | The Railway diagnostic output exposed these credential rows by variable name: `ANTHROPIC_API_KEY`, `ELEVENLABS_API_KEY`, and `OPENWEATHER_API_KEY`. | Rotate those three staging keys before any further work uses them. The same diagnostic also printed `ELEVENLABS_VOICE_ID`, `EXPECTED_GIT_SHA`, `GIT_SHA`, and `NEXTEAM_DEPLOY_SHA`, which are identifiers/commit SHAs rather than secrets. `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `GOOGLE_MAPS_API_KEY`, Gmail OAuth secrets/tokens, Firebase private keys, Jobber credentials, CompanyCam credentials, and WordPress credentials were not printed by that diagnostic output. | Internal tool output only; no secret values repeated in chat or committed. |
| 2026-07-09T09:18:00-04:00 | security | Secret-output exposure is a command-output class defect, not a provider-specific defect. | CLIs and file-search commands can print raw env/config/source text; generic shell output goes straight into the Codex tool transcript. Redaction after the assistant sees output is too late, and prior wrapper redaction only knew about Railway auth tokens. Added shared safe-output redaction and a generic safe command wrapper so risky diagnostics can capture child output in-process and emit only redacted text. | `scripts/security/SafeOutput.ps1`, `scripts/security/invoke-safe-command.ps1`, `scripts/security/invoke-railway-staging.ps1` |
| 2026-07-09T04:33:00-04:00 | integration | The fully integrated branch deployed to staging at SHA `103fdba85a95e67312b98268cf0633c3f009c32b`, but the live regression wall is blocked by Anthropic billing. | `/api/version` matches the deployed code SHA and `/api/health` is green for Jobber and CompanyCam; a 5-case live wall probe running inside Railway staging failed only because `/api/nexi/message` returned Anthropic credit-balance errors. | `receipts/integration/nightly-integration-live-wall-blocked-20260709.json` |
| 2026-07-08T22:20:00-04:00 | reputation | M7 is built locally to the ApprovalQueue boundary but remains blocked for the live GBP receipt until owner OAuth/account/location setup is complete. | The Bible receipt requires a real GBP review fetch and approved reply visible on GBP. Without tenant GBP OAuth credentials and location identifiers, the conservative behavior is an explicit blocker, not a fake empty review queue. | `receipts/m7/reputation-local-receipt-20260708.json` |
| 2026-07-08T22:20:00-04:00 | seo | M9 is implemented as a local-safe, ApprovalQueue-gated module, but live done status is blocked. | DataForSEO credentials are not available in this worktree/staging context, and the Rich Results validation screenshot must be captured in a browser; local tests return an explicit DataForSEO blocker instead of fake ranks. | `receipts/m9/seo-local-receipt-20260708.json` |
| 2026-07-08T23:18:00-04:00 | voice | M12b starts with an interruptible hands-free session layer over the existing ElevenLabs TTS rail, not a tenant-facing avatar provider integration. | Owner has not chosen HeyGen or D-ID yet, and Part 9 forbids calling M12b done from backend-only proof. The code now tracks provider-agnostic sessions, interruption, first-audio timing, and cost metadata; full done waits for staging deploy, real browser hands-free video, usageLog proof, and green wall. | `apps/server/src/voice/fullDuplex.ts`, `apps/web/src/main.tsx`, `receipts/m12b/m12b-full-duplex-local-receipt-20260708.json` |
| 2026-07-08T22:18:00-04:00 | intake | M10 tenant onboarding provisions only native NexTeam tenant records after ApprovalQueue execution. | The onboarding plan includes OAuth steps, site draft seed, templates, and app-stack decisions, but external account setup, sends, publishing, domains, and deploys remain owner-action/deferred items. This preserves ApprovalQueue-only outbound rules while making the onboarding path testable. | `apps/server/src/intake/service.ts`, `apps/server/src/intake/approvalExecutor.ts`, `receipts/m10/intake-local-receipt-20260708.json` |
| 2026-07-08T22:37:00-04:00 | intake | M10 onboarding commands get deterministic chat routing for start/finalize intent. | Tenant onboarding is a high-risk action path and must not drift into create-client, job lookup, or email search based on phrasing; deterministic routing keeps natural owner prompts on the intake rail while provisioning remains ApprovalQueue-gated. | `apps/server/src/nexi/nexiService.ts`, `apps/server/test/intake.test.mjs`, `receipts/m10/intake-local-receipt-20260708.json` |
| 2026-07-10T16:20:00-04:00 | reputation | GBP production variable inspection is blocked by token scope, not by code. | The only local Railway DPAPI vault is `railway-staging.dpapi`. It can read staging variable names, where all GBP current and legacy names are absent, but returns `Unauthorized` when explicitly pointed at production. A separate production-scoped token must be captured before production env names can be checked or mirrored. | `docs/sessions/lanes/aquatrace-m7-reputation-gbp.md`, `SESSION_HIERARCHY.md` |
| 2026-07-10T16:20:00-04:00 | operations | Codex sessions are organized by the approved company/tenant/module hierarchy. | Root `SESSION_HIERARCHY.md` is now the pinned entry point, lane files have `Last updated` fields, and the active Bible/build thread plus the GBP rail thread were renamed to the approved convention. | `SESSION_HIERARCHY.md`, `docs/sessions/lanes/aquatrace-m7-reputation-gbp.md`, `docs/sessions/lanes/platform-m13-multi-user.md` |
| 2026-07-10T16:45:00-04:00 | phase1 | Phase 1 master specs are now build-governing docs and locked to the Nex-name family. | Before any Phase 1 business-engine or field-documentation build, agents must read both stored specs and follow the ask-first protocol. Locked names: NexOps business engine, NexCam field documentation, NexPortal client hub, NexReach marketing/reputation/campaign engine, Nexi assistant, NexTeam platform. | `docs/specs/phase1/NEXTEAM-PHASE1-MASTER-SPEC.md`, `docs/specs/phase1/NEXTEAM-FIELDDOCS-MASTER-SPEC.md`, `SESSION_HIERARCHY.md`, `AGENTS.md` |
| 2026-07-10T17:22:00-04:00 | operations | Maintain `CLAUDE_BUILD_HANDOFF.md` as the daily Claude/Nova upload file for this worktree. | Owner wants one full build file updated on every meaningful pass, reply, input, receipt, blocker, and decision so daily progress can be handed to Claude without reconstructing chat history. | `CLAUDE_BUILD_HANDOFF.md` |
| 2026-07-26T23:40:00-04:00 | architecture | CRM HTTP ownership is expressed through component registrars; `crm/routes.ts` only creates the runtime context and invokes the registrar manifest. | Moving each existing Express call intact preserves behavior while ending shared-file collisions. The broad typed runtime context is intentionally transitional: E4 narrows dependencies as business logic moves into each component, so it cannot become a replacement monolith. | `apps/server/src/crm/routes.ts`, `apps/server/src/crm/routeRuntime.ts`, `apps/server/src/modules/nexops/areas/*/components/*/server/routes.ts`, `receipts/architecture/e3-routes.txt` |
| 2026-07-27T00:05:00-04:00 | architecture | CRM assistant capabilities are registered by component manifests and collected by a compatibility entrypoint. | Each Nexi tool handler now changes with its owning business component rather than forcing every CRM feature through one 2,282-line file. Read/write factory signatures remain stable for callers while E4 narrows the temporary shared runtime context. | `apps/server/src/crm/nexiTools.ts`, `apps/server/src/crm/nexiToolRuntime.ts`, `apps/server/src/modules/nexops/areas/*/components/*/server/nexiTools.ts`, `receipts/architecture/e3-nexi-tools.txt` |
| 2026-07-27T00:25:00-04:00 | architecture | Firestore CRM persistence is composed from component-owned stores behind the existing repository constructor. | Repository consumers should not change while component teams gain separate implementation files. Contact, Request Core, Tenant Config, Quote Templates, Job Core, Quote Engine, Invoice Structure, and shared Numbering own their methods; the root file only composes the complete interface. | `apps/server/src/crm/nativeRepository.ts`, `apps/server/src/crm/firestoreRepositoryBase.ts`, `apps/server/src/modules/nexops/areas/*/components/*/server/firestoreRepository.ts`, `apps/server/src/shared/numbering/firestoreNumberingRepository.ts` |
| 2026-07-27T03:00:00-04:00 | architecture | Job Core orchestrates Visit Core through a stable service boundary; Visit Core owns scheduling, movement, completion, reminder dispatch, and reminder persistence. | Visit behavior changes independently from job lifecycle policy. Reminder reads use a non-reentrant job-detail callback so due-reminder processing can resolve client/property context without recursively starting itself. Existing Job service callers retain compatibility delegation while implementation ownership is disjoint. | `apps/server/src/modules/nexops/areas/jobs/components/jobCore/server/jobLifecycleService.ts`, `apps/server/src/modules/nexops/areas/visits/components/visitCore/server/visitCoreService.ts`, `apps/server/src/modules/nexops/areas/visits/components/visitCore/server/visitReminderRepository.ts`, `receipts/architecture/e4-job-visit-complete.txt` |
| 2026-07-27T04:00:00-04:00 | architecture | Invoice Structure owns invoice policy and documents; Payment Rails consumes that policy and owns provider/payment execution. | Draft construction, totals, schedules, collection eligibility, void/bad-debt status history, and customer-facing invoice wording are billing policy. Stripe, PayPal/Venmo, Tap to Pay, refunds, and payment records are execution. Shared portal chrome remains in Document Rendering so quote and invoice templates do not duplicate shell behavior. | `apps/server/src/modules/nexops/areas/invoices/components/invoiceStructure/domain/invoicePolicy.ts`, `apps/server/src/modules/nexops/areas/invoices/components/invoiceStructure/server/invoiceDocument.ts`, `apps/server/src/shared/documentRendering/portalChrome.ts`, `receipts/architecture/e4-invoice-structure.txt` |
| 2026-07-27T04:30:00-04:00 | architecture | Payment Rails owns provider execution UI and is composed by Invoice Structure through typed state and callback props. | Editing invoice wording, schedules, or delivery should not require touching Stripe/PayPal/manual collection controls; changing payment recovery should not require editing the invoice workspace implementation. The invoice page remains the state orchestrator but contains no provider-control JSX. | `apps/web/src/features/invoices/components/paymentRails/PaymentRailsPanel.tsx`, `apps/web/src/features/invoices/components/invoiceStructure/NexOpsInvoicesPage.tsx`, `receipts/architecture/e4-payment-rails-complete.txt` |
| 2026-07-27T05:10:00-04:00 | architecture | Catalog writes belong only to Settings; Quotes and Invoices consume the Catalog through a read-only picker. | Catalog is a reusable source of line items, not a document-owned editor. This prevents quote or invoice work from changing Catalog implementation files and makes the Settings ownership boundary visible to users instead of duplicating write paths. | `apps/web/src/features/settings/components/catalog/NexOpsCatalog.tsx`, `apps/web/src/features/quotes/components/quoteEngine/NexOpsQuotesPage.tsx`, `apps/web/src/features/invoices/components/invoiceStructure/NexOpsInvoicesPage.tsx` |
| 2026-07-27T05:10:00-04:00 | architecture | Quote and invoice customer documents have component-owned renderers behind the legacy `crm/quotePdf.ts` export facade. | Existing callers keep a stable import path while Quote Engine and Invoice Structure can change their own wording and layouts independently. Portal chrome and the broadly reused `.nexops-quote*` layout rules remain shared Document Rendering/design primitives rather than being falsely assigned to either component. | `apps/server/src/crm/quotePdf.ts`, `apps/server/src/modules/nexops/areas/quotes/components/quoteEngine/server/quoteDocument.ts`, `apps/server/src/modules/nexops/areas/invoices/components/invoiceStructure/server/invoiceDocument.ts`, `apps/web/src/features/nexopsShell/documentPrimitives.css` |
| 2026-07-27T06:20:00-04:00 | architecture | Generic runtime code never invents a tenant; local/test runtimes opt into one explicitly. | A literal fallback can silently route authenticated or admin-backed work into the wrong tenant. Server routes and providers now require request/auth context or `TENANT_ID`; web/mobile require claims or explicit runtime configuration. The test bootstrap names its fixture tenant and enables memory stores explicitly, so test convenience cannot become production behavior. | `apps/server/src/core/tenantConfig.ts`, `apps/server/src/auth/accessContext.ts`, `tests/setup.mjs`, `receipts/architecture/e5-baseline-sanitization.txt` |
| 2026-07-27T06:20:00-04:00 | architecture | Tenant-owned content and operational material remains available only behind explicit tenant-pack paths. | Removing Aquatrace behavior would be a regression, while leaving it in generic modules would make the product baseline tenant-specific. Compatibility facades preserve imports as the branded implementations, assets, campaign fixtures, and operations documents move under `tenantPacks/aquatrace` or `docs/tenants/aquatrace`. | `apps/server/src/tenantPacks/aquatrace/`, `packages/industry-packs/src/tenantPacks/aquatrace/`, `apps/web/public/tenant-packs/aquatrace/`, `docs/tenants/aquatrace/operations/` |
| 2026-07-27T08:10:00-04:00 | architecture | The integrated server fails closed based on actual durable-repository availability, not an environment-name flag. | Firebase-backed ApprovalQueue, Content, and Scheduling repositories are selected automatically when Admin credentials are available. Missing durability blocks startup unless an explicitly non-production runtime sets `ALLOW_IN_MEMORY_PERSISTENCE=true`, so forgetting a production-mode label cannot silently enable memory storage. | `apps/server/src/app/persistencePolicy.ts`, `apps/server/src/app/runtime.ts`, `apps/server/src/composeServerApp.ts`, `apps/server/test/platform.test.mjs` |
| 2026-07-27T08:10:00-04:00 | architecture | Executable startup, dependency composition, HTTP registration, and integrated Nexi tool registration are separate boundaries. | `server.ts` only listens; the composition root constructs services; system/auth/media/approval and feature registrars own HTTP handlers; the Nexi registrar owns request-aware tool assembly. This keeps feature wiring out of the executable bootstrap without hiding behavior behind a facade. | `apps/server/src/server.ts`, `apps/server/src/composeServerApp.ts`, `apps/server/src/core/systemRoutes.ts`, `apps/server/src/nexi/integratedRoutes.ts` |
| 2026-07-27T08:10:00-04:00 | architecture | Compound Firestore queries have a checked deployment contract. | The integrated branch has ten current multi-field query shapes. Keeping all ten in `firestore.indexes.json` and verifying their signatures prevents a code merge from depending on an index that deployment configuration omits. | `firestore.indexes.json`, `scripts/check-firestore-indexes.mjs` |
| 2026-07-27T03:20:00-04:00 | architecture | The NexOps workspace orchestrator is an intentional app-shell boundary, not a Contact component. | The previous function was named `NexOpsClientsPage` but owned navigation, module routing, capture, notifications, and every NexOps area. Moving it intact out of `main.tsx` makes that shared ownership explicit and creates a stable place from which each real area surface can be extracted without mislabeling the shell as Contact. | `apps/web/src/features/nexopsShell/NexOpsWorkspace.tsx`, `apps/web/src/main.tsx`, `receipts/architecture/e4-web-app-shell.txt` |
| 2026-07-27T04:10:00-04:00 | architecture | Contact profile rendering is component-owned while the shared app shell retains data orchestration. | Client profile state coordinates shared navigation, NexDocs, payments, requests, and notifications, so moving state wholesale would make Contact the new shell. A closed named binding seam lets Contact own every client-facing surface and its CSS while the shell owns only cross-area orchestration. | `apps/web/src/features/clients/components/contact/`, `apps/web/src/features/nexopsShell/NexOpsWorkspace.tsx`, `receipts/architecture/e4-contact-complete.txt` |
| 2026-07-27T00:45:00-04:00 | architecture | CRM ApprovalQueue execution dispatches to component-owned handlers after a service/operation allow-list check. | Approval execution is a high-risk write boundary. The root executor retains explicit rejection of unknown operations, while each component owns argument validation, tenant equality checks, and the actual write for its operations. | `apps/server/src/crm/approvalExecutor.ts`, `apps/server/src/crm/approvalContracts.ts`, `apps/server/src/crm/approvalHandler.ts`, `apps/server/src/modules/nexops/areas/*/components/*/server/approvalHandler.ts` |
| 2026-07-27T01:05:00-04:00 | architecture | Lifecycle command and communication policy is aggregated from owner manifests; D1-D19 and permission IDs remain shared governance. | Commands, message contracts, and dominant-action rules change with their business component, while cross-component decision IDs remain one auditable policy registry. Existing imports keep the aggregate API and all referential-integrity tests. | `apps/server/src/crm/lifecycleCommandMap.ts`, `apps/server/src/crm/lifecyclePolicy.ts`, `apps/server/src/modules/nexops/areas/*/components/*/server/lifecycleContracts.ts`, `apps/server/src/modules/nexops/areas/*/components/*/domain/dominantAction.ts` |
| 2026-07-27T11:30:00-04:00 | architecture | Remaining web behavior is assigned by functional ownership, not by its legacy route-brand container. | The `/nexcam` function contains NexDocs checklist/media/report behavior, and standalone Nexi contains a voice area plus shared shell chrome. Moving either function intact would only relocate a monolith. NexCam and Nexi may compose exported owner surfaces, while NexDocs, voice, auth/session, operator context, and shell primitives retain independent implementation roots. | `receipts/architecture/r1-r2-web-boundary-map.txt` |
| 2026-07-27T11:31:00-04:00 | architecture | Existing product-visible routes are repaired during extraction; absent product routes are not invented. | The approvals navigation already resolves to an undefined `ApprovalQueuePanel`, so restoring its real server-backed surface is a bug fix. Content Queue has server behavior but no live web route, so its surface will be owner-extracted and left unmounted until product navigation is explicitly decided. | `apps/web/src/features/nexopsShell/NexOpsWorkspace.tsx`, `receipts/architecture/r1-r2-web-boundary-map.txt` |
| 2026-07-27T09:46:00-04:00 | architecture | Operator tenant/user/role claim resolution is a shared authenticated-context component, not NexOps, Nexi, NexCam, or NexReach implementation. | Four modules require the same authenticated context. A single resolver prevents module shells from importing tenant policy from one another and removes the unused competing hook without changing the explicit local tenant configuration boundary. | `apps/web/src/features/operatorContext/resolveOperatorContext.ts`, `receipts/architecture/r3-operator-context.txt` |
| 2026-07-27T10:05:00-04:00 | architecture | Collision proof distinguishes component ownership from an explicit shared composition/engine allowlist. | A zero-overlap claim is meaningful only when shared files are visible. The 28-component gate names app/router/NexOps shells, every root-level web compatibility/composition file, and existing Nexi/Field Docs server engines instead of hiding them inside two component sets. It fails on any new unclassified root web file; expanding the allowlist requires an architecture decision. | `scripts/check-component-collisions.mjs`, `receipts/architecture/r4-all-component-proof.txt` |
| 2026-07-27T10:35:00-04:00 | testing | Browser tests are opt-in by filename, command, and a fail-fast default-suite guard. | A mixed Node/Playwright file allowed an exclusion mistake to launch Chromium. Browser cases now use `*.playwright.mjs`, which the standard glob cannot match; `npm test` also rejects browser imports and launch calls in every default `*.test.mjs` file before execution. | `package.json`, `scripts/check-no-browser-tests.mjs`, `apps/web/browser-tests/nexi-standalone-ui.playwright.mjs`, `receipts/architecture/no-browser-default-tests.txt` |
| 2026-07-27T10:55:00-04:00 | architecture | Nexi presentation/session and request-origin helpers belong to Nexi Chat, not the root compatibility allowlist. | These files are changed by chat behavior and have one real owner. Contact consumes only the stable maps helper; that dependency does not make the implementation shared ownership. | `apps/web/src/features/nexi/areas/chat/components/NexiStandalonePrimitives.tsx`, `apps/web/src/features/nexi/areas/chat/utils/nexiRequestContext.ts`, `receipts/architecture/root-debt-nexi-ownership.txt` |
| 2026-07-27T13:20:00-04:00 | architecture | The legacy lifecycle fallback, import workflow, and module switcher are shell surfaces, not workspace-orchestrator implementation. | Each surface now receives state and actions through a typed prop boundary. The workspace retains route/state coordination but no longer owns their markup or presentation rules, so surface edits do not require changing the orchestrator. | `apps/web/src/features/nexopsShell/components/NexOpsLegacyLifecyclePage.tsx`, `apps/web/src/features/nexopsShell/components/NexOpsImportPage.tsx`, `apps/web/src/features/nexopsShell/components/NexOpsModuleSwitcher.tsx`, `receipts/architecture/nexops-workspace-surfaces.txt` |
| 2026-07-27T14:00:00-04:00 | architecture | NexCam Capture owns its stateful web controller and capture-specific contracts; NexOps Shell supplies navigation callbacks only. | Upload, assignment, inbox, and session behavior changes with capture policy and must not require editing the product workspace. Generic browser-file encoding has a separate shared owner because both NexCam and Nexi use it. | `apps/web/src/features/nexcam/areas/capture/hooks/useNexOpsCaptureController.ts`, `apps/web/src/features/nexcam/areas/capture/contracts/captureContracts.ts`, `apps/web/src/shared/files/fileEncoding.ts`, `receipts/architecture/nexcam-capture-controller.txt` |
| 2026-07-27T14:30:00-04:00 | security | Scheduling repository writes must reject cross-tenant document-ID collisions at the persistence boundary. | Firebase Admin bypasses Firestore rules, so request-layer tenant checks are insufficient. Ownership validation and mutation now share one transaction, preventing a checked record from changing before the write and preventing an Admin caller from overwriting another tenant's visit. | `apps/server/src/scheduling/repository.ts`, `apps/server/test/scheduling-tenant-boundary.test.mjs`, `receipts/architecture/admin-isolation-scheduling.txt` |
| 2026-07-27T15:00:00-04:00 | architecture | Notification persistence and refresh are a NexOps Shell controller; target routing remains workspace orchestration. | The notification rail has one tenant-bound API lifecycle but can target several business modules. Keeping API state in a shell hook and injecting a target-navigation callback avoids assigning cross-module routing to Notifications or pushing notification persistence back into the workspace. | `apps/web/src/features/nexopsShell/hooks/useNexOpsNotifications.ts`, `receipts/architecture/nexops-notification-controller.txt` |
| 2026-07-27T15:35:00-04:00 | security | Admin-backed repositories use a shared transactional tenant-owned-write seam for same-ID writes. | Content exposed the same collision class across five collections. A shared primitive now checks the stored tenant and writes in one transaction; repositories still own schema-specific parsing and immutable fields. This seam is the migration path for remaining Admin repositories, not a claim that all are already covered. | `apps/server/src/core/tenantOwnedWrite.ts`, `apps/server/src/content/repository.ts`, `apps/server/test/content-tenant-boundary.test.mjs`, `receipts/architecture/admin-isolation-content.txt` |
| 2026-07-27T16:00:00-04:00 | architecture | Cross-area NexOps read models and global CRM refresh belong to a shell controller, not Contact. | Properties, jobs, quotes, invoices, users, requests, payments, and receipt reviews support several modules. The shell controller owns their loading while selected-client routing is injected; client portal/review/NexDocs rails remain Contact-specific for the next extraction. | `apps/web/src/features/nexopsShell/hooks/useNexOpsWorkspaceRecords.ts`, `receipts/architecture/nexops-workspace-records.txt` |
| 2026-07-27T16:25:00-04:00 | security | Reputation reviews and profiles use transactional tenant-owned upserts rather than unrestricted Admin merge writes. | Provider IDs are not guaranteed globally unique across tenants. The stored owner must be checked before a same-ID review or profile is replaced, and memory mode must preserve the same boundary. | `apps/server/src/reputation/repository.ts`, `apps/server/test/reputation-tenant-boundary.test.mjs`, `receipts/architecture/admin-isolation-reputation.txt` |
| 2026-07-27T17:00:00-04:00 | architecture | Contact owns client form and client-specific rail orchestration; NexOps Shell owns only cross-module route transitions. | Validation, payload construction, persistence, portal/review/NexDocs rails, custom fields, and guarded mutations change with Contact behavior. Injected refresh and navigation callbacks keep those implementations out of the shared workspace without assigning cross-area routing to Contact. | `apps/web/src/features/clients/components/contact/hooks/useContactFormController.ts`, `apps/web/src/features/clients/components/contact/hooks/useContactClientRails.ts`, `receipts/architecture/contact-form-rails-controller.txt` |
| 2026-07-27T17:30:00-04:00 | security | ApprovalQueue access is tenant-first at the API, service, and persistence layers. | Looking up an approval by global ID before authorization leaks tenant context and leaves Admin-backed mutations dependent on caller discipline. Every read/decision/execution now carries the already-known tenant, and Firestore verifies that owner in the same transaction as each write. | `packages/core/src/approvalQueue.ts`, `apps/server/src/approval/firestoreRepository.ts`, `apps/server/src/approval/routes.ts`, `apps/server/test/approval-tenant-boundary.test.mjs` |
| 2026-07-27T18:00:00-04:00 | security | Caller-selected document IDs use transactional tenant ownership checks; append-only generated IDs are classified separately. | Admin SDK bypasses rules, so schema presence alone cannot protect an upsert from replacing another tenant's document. Append-only UsageLog writes cannot target an existing ID and therefore retain generated IDs plus tenant schema validation rather than a needless pre-read. | `apps/server/src/core/tenantOwnedWrite.ts`, `apps/server/src/intake/repository.ts`, `apps/server/src/sites/repository.ts`, `apps/server/src/seo/repository.ts`, `apps/server/src/usageLog.ts`, `receipts/architecture/admin-isolation-operational-repositories.txt` |
| 2026-07-27T18:30:00-04:00 | security | Raw native CRM updates require explicit tenant context; NativeAdapter remains the tenant-bound convenience boundary. | ID-only repository updates are unsafe even when most provider calls pre-check ownership. Requiring tenantId in the repository patch lets TypeScript expose every direct caller and lets Firestore verify the stored owner transactionally, while provider-facing methods continue to inject the adapter's already-verified tenant. | `packages/providers/src/native/NativeAdapter.ts`, `apps/server/src/modules/nexops/areas/*/components/*/server/firestoreRepository.ts`, `apps/server/src/core/tenantOwnedWrite.ts`, `receipts/architecture/admin-isolation-crm-repositories.txt` |
| 2026-07-27T19:00:00-04:00 | security | Direct or unclassified Firebase Admin writes are a CI failure. | Repository-level tenant checks are easy to bypass accidentally because Admin SDK ignores Firestore rules. `check:admin-tenant-writes` scans every server/core TypeScript source, rejects direct document/batch/ref writes, requires a read plus tenant assertion for transactions, and permits only the generated-ID schema-validated UsageLog append. | `scripts/check-admin-tenant-writes.mjs`, `package.json`, `receipts/architecture/admin-isolation-final-repositories-static.txt` |
| 2026-07-27T19:30:00-04:00 | security | Admin SDK isolation completion requires an opt-in Firestore emulator proof, not only fake transaction harnesses. | The emulator executes the production repository code against Firestore's real transaction behavior without credentials or live data. It proves representative shared and custom transaction boundaries while remaining separate from normal tests so local verification is deterministic and cannot silently start infrastructure. | `apps/server/test/admin-tenant-isolation.emulator.test.mjs`, `scripts/run-admin-tenant-isolation-emulator-tests.mjs`, `receipts/architecture/admin-isolation-emulator.txt` |
