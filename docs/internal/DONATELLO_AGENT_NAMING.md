# Donatello engineering-agent naming

Splinter is the controller and Raphael is the independent reviewer. Donatello is the current NexTeam technical builder responsible for implementation and controlled repair under Splinter authority.

`Atlas` is a legacy compatibility alias only. Historical records, Git history, stored evidence, legacy bridge packets, and existing internal identifiers must not be rewritten. Current user-facing and new operational references use `Donatello`; compatible transport may accept `atlas` until the alias can be retired after verified staging operation.
